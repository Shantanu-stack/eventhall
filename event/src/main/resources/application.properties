spring.application.name=demo
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/eventhall_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:1234}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8080
```

Your password `1234` is kept. Local dev still works same. ✅

---

## Change 3 — Create new file `frontend\.env.docker`
Create this file with content:
```
VITE_API_URL=/api